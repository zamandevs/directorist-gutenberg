/**
 * Listings Archive Search Block - Frontend Script
 * Column preservation is handled by the preserve-column-structure utility
 * which is loaded via blocks-frontend.js
 */

// The utility auto-initializes when loaded, so no action needed here
if (typeof window.directoristPreserveColumnStructure !== 'undefined') {
	// Utility is already initialized and will handle everything automatically
}
